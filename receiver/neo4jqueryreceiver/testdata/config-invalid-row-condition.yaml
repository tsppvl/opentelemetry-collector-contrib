neo4j_query:
  uri: bolt://localhost:7687
  queries:
    - cypher: "RETURN 1 AS one, 'a' AS name"
      metrics:
        - metric_name: one
          value_column: one
          row_condition:
            value: a
